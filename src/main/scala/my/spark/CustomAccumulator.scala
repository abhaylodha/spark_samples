package my.spark

import my.spark.common_utils.SparkRunner
import org.apache.spark.sql.SparkSession

object CustomAccumulator extends SparkRunner {
  def run(spark: SparkSession, args: Array[String]): Unit = {
    
  }
}
